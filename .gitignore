# Node
node_modules/
npm-debug.log*
.pnpm-debug.log*

# Rust / Cairo / Scarb
target/
**/*.rs.bk
# Scarb.lock SE VERSIONA a propósito — no ignorar

# Env / OS
.env
.DS_Store
*.swp

# sncast / Starknet Foundry — config de máquina, no portátil
snfoundry.toml
pending_review.md

# Keystores y archivos de clave
*.keystore
*.key
*.pem
